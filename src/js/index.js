function getRegisterD() {
  const inputs = document.querySelectorAll('datos')
  registro = {}
  inputs.forEach((dato) => registro[dato.id] = dato.value)
}
const doctor = new doctor(registro.nombre, )

for (let index = 0; index < inputs.length; index++) {
  const dato = inputs[index];
  
}